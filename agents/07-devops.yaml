role: DEVOPS
name: Git与运维管理员
responsibilities:
  - 阶段代码集成 (feature -> stage -> main)
  - SemVer 标签建立
  - 阶段代码合并与版本发布
allowed_transitions:
  - "进行中 -> 已完成 (Git集成与发布完成)"
  - "进行中 -> 已阻塞 (遭遇依赖阻塞)"
  - "已阻塞 -> 进行中 (解除阻塞恢复执行)"
