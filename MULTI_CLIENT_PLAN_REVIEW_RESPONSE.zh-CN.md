# 关于《Multi-Agent Flow 多客户端可信化与生态改造实施方案》的评审意见与最终决议

> **评审对象**：`MULTI_CLIENT_MODERNIZATION_PLAN.zh-CN.md`  
> **适用仓库**：`YuanYii/multi-agent-flow`  
> **评审日期**：2026-08-24  
> **评审结论**：**部分采纳，拒绝过度设计，坚守本地敏捷与强审计核心护城河**  

---

## 1. 事实基线校正 (Fact Correction)

原方案第 2 节与第 7.1 节指出：
> *“当前测试无法完成收集，直接原因是 `scripts/start_kanban_server.py` 使用了 `Any`、`Optional` 但未从 `typing` 导入”*

**事实校正**：
- 主仓在先前的架构重构中已彻底修复类型导入并完成 CSRF 与回环安全加固；
- 当前代码库全量测试套件为 **220 项全部通过（`220 passed in 25.89s`）**，基线处于稳定全绿状态，无需将“修复导入错误”作为新的阶段任务。

---

## 2. 核心争议项评审结论与决策依据 (Core Decisions)

经过架构与工程可行性综合评估，对原方案中的核心争议点做出以下明确决议：

### 2.1 任务作废机制：坚守【只取消、不删除】，拒绝引入软删除与回收站
- **原方案主张 (Sec 7.3)**：`DELETE` API 改为打标 `is_deleted: true`，增加回收站与 `restore` 恢复接口。
- **评审决议**：❌ **不采纳 / 保持现状**
- **决策依据**：
  1. **强审计哲学**：Multi-Agent Flow 是严谨的工程研发流转体系，工单具备不可篡改性与连续性，不支持增删改查（CRUD）式的随意物理抹除；
  2. **原生态【已取消】闭环**：系统底层 `validate_transition.py` 原生支持从任意阶段推向【已取消】终态。任务写错或需求废弃时，必须调用 `transition_task.py --to-status 已取消 --remarks "作废原因"`，经办人收敛至 PM 严经理并记录时间戳；
  3. **门禁兼容**：阶段门禁 `check_stage_gate.py` 原生将【已验收】与【已取消】共同作为合法结项终态，无断号风险。

### 2.2 云端与多租户架构：坚守【本地优先 + 桥接成熟云端】，拒绝自建云端 SaaS 与 OAuth
- **原方案主张 (Sec 10.2–10.3)**：自建多租户数据库、`tenant_id` 隔离、云端 API、自建 OAuth 服务与 ChatGPT Web Plugin。
- **评审决议**：❌ **不采纳 / 永久废弃**
- **决策依据**：
  1. **局域网协同已就绪**：`start_kanban_server.py` 与底层文件锁原生支持局域网内多设备、多人/多 Agent 并发访问与实时看板展示；
  2. **直接复用成熟云端生态**：系统通过 `board_adapter_factory.py` 已经原生集成了 **飞书多维表格（Feishu Base）、GitHub Projects v2、Jira** 3 大成熟企业级云端看板，天然具备企业级权限隔离与协同能力，无需从零自建一套脆弱且高运维成本的云端 SaaS 后端。

### 2.3 自动化模式：升级为【证据驱动 Auto-Runner】，拒绝阉割为纯模拟
- **原方案主张 (Sec 7.5)**：将 `/auto` 和 `auto_task.py` 改为只输出计划、禁止写数据的纯模拟（Simulation）模式。
- **评审决议**：❌ **不采纳**
- **决策依据**：
  - 用户使用 `/yy-flow auto` 的核心价值在于端到端自主流水线推进。阉割为只能打印文本的纯模拟会破坏自动化体验；
  - **正确做法**：保持真实自动化，在流转底层注入**物理证据硬核验**（执行单测拿到 `exit_code: 0` 才允许写卡推进，无证据自动拦截）。

### 2.4 工作区与环境隔离：坚守【主工作区直接编码】，拒绝外部 Git worktree
- **原方案主张 (Sec 8.4)**：强制在外部临时目录拉取独立 Git worktree 进行子 Agent 隔离编码。
- **评审决议**：❌ **不采纳**
- **决策依据**：
  1. **IDE 体验割裂**：开发者在 IDE 打开的是主工程，外部隐藏 worktree 编码会导致 IDE 无法实时感知变动、无法打断点调试；
  2. **依赖环境丢失**：新拉取的外部 worktree 缺少主项目的 `node_modules`、Python `.venv` 虚拟环境与本地配置，直接导致构建和测试抛出模块缺失异常；
  3. **Windows 锁死风险**：在 Windows 环境下极易因进程锁导致 worktree 无法清理而堆积垃圾目录。

### 2.5 技能包架构：坚守【单一高内聚 `yy-flow`】，拒绝拆分为 6 个碎片化小 Skill
- **原方案主张 (Sec 9.1)**：拆分为 `yy-flow-router`、`yy-flow-task`、`yy-flow-review` 等 6 个独立小包。
- **评审决议**：❌ **不采纳**
- **决策依据**：
  - 技能包各组件高度协同，拆分后破坏了原子协同机制，安装升级极为繁琐（从 1 行命令变成装 6 个包）；
  - 多个同质化小 Skill 极易导致大模型在自然语言理解时发生“技能选择漂移”或漏调。

### 2.6 人机交互确认：遵循【AGENTS.md 授权例外】，拒绝机械双重弹窗阻断
- **原方案主张 (Sec 6.1 & 8.7)**：任何任务开工前强制停机等待用户确认、完工前再次停机等待用户确认。
- **评审决议**：❌ **不采纳**
- **决策依据**：
  - 机械的双重确认会导致批量执行与 Auto 模式彻底瘫痪，沦为“弹窗确认地狱”；
  - 遵循项目最高规则 **AGENTS.md 规则 3**：*“初始指令已显式授权的任务，执行过程中无需再次授权；仅在涉及删除文件、改配置等高风险操作时单独弹窗确认”*。

### 2.7 专家角色与人设：坚决保留【见名知意的中文专家姓名】
- **原方案主张 (Sec 9.3)**：抹除“李开发、周审查、严经理、马前端”等中文姓名，改为抽象代码。
- **评审决议**：❌ **不采纳**
- **决策依据**：
  - 中文姓名与专家职责一一映射（李开发负责编码、周审查负责代码审查、章测试负责质量测试、严经理负责把控验收），起到了**见名知意、权责直观**的效果，是多专家协同的核心产品体验。

### 2.8 工作量评估：不套用传统人工工日估算
- **原方案主张 (Sec 12)**：估算该现代化改造需要 28~54 人日。
- **评审决议**：❌ **不采纳**
- **决策依据**：
  - 当前 AI 协同研发效率极高，不应套用传统人工开发的漫长工日；
  - 剔除上述大量被否决的云端多租户、外部 worktree 等过度设计后，真正有价值的轻量演进仅需 **1 天以内** 即可交付。

### 2.9 流程轻量分级 (Lite/Standard)：直接复用原生 L0 与 A~G 任务机制，拒绝冗余 Profile 重构
- **原方案主张 (Sec 9.2)**：专门开发 Lite 模式和 Standard 模式的新字段与新状态机逻辑。
- **评审决议**：❌ **不采纳 / 保持现状**
- **决策依据**：
  1. **系统早已原生支持**：`01-pm.yaml` 早已内置“派单前三问判定（L0 纯问答直接作答免建卡，L1 短链单角色，L2 全链多角色）”；
  2. **任务分类天然支持短链**：`validate_transition.py` 原生支持 B 类（文档）、C 类（缺陷修复）、G 类（环境配置）短链跳过审查/测试直达【已完成】，大功能走 A 类全链；
  3. **避免过度设计**：额外增设 Profile 概念与既有任务分类严重重叠，徒增字段与改造成本，直接沿用既有轻量机制即可。

---

## 3. 确立 Multi-Agent Flow 两大核心设计哲学

针对实际多 Agent 研发协作，正式确立以下两大黄金准则：

1. **原则 1：Agent 职责单一，Git 提交统一由 DevOps（吕改特）集中收敛**
   - 业务开发 Agent（李开发、马前端）专注在工作区产出高质量业务代码与单测，不考虑多分支代码冲突适配；
   - 所有 Git 提交、分支合并与发布 Tag 统一在阶段门禁结项时由 **吕改特 (`@flow-devops`)** 集中执行。
2. **原则 2：严把代码质量不走 Auto，按任务顺序单步推进并在 IDE 中用 Git Diff 工具肉眼严审**
   - 敏捷快跑与常规批量任务 ➔ 走 `/yy-flow auto` 自动流水线；
   - 核心复杂重构与高质量监控 ➔ 禁用 Auto 模式，按任务顺序单步推进，开发者直接在 **IntelliJ IDEA / VS Code 自带的 Git 双栏比对工具（Cmd+D）** 中进行逐行肉眼把关与断点调试。

---

## 4. 确认采纳并纳入后续轻量演进的清单 (Accepted Items)

除上述否决与澄清项外，原方案中以下具有实际工程价值的建议予以**正式采纳**：

| 采纳功能点 | 具体设计与实施方案 | 预计投入 |
| :--- | :--- | :---: |
| **1. 物理证据链存盘 (Evidence)** | 状态流转强绑定真实测试退出码（`exit_code: 0`）与 Git 事实凭据，在 `.yy-flow/user_data/evidence/` 落盘结构化证据快照。 | 0.5 天 |
| **2. 项目身份防串机制 (`project.json`)** | 初始化时在项目私有目录生成 `.yy-flow/project.json`（记录 `project_id` 与根路径），防止全局安装模式下误串项目。 | 0.5 天 |

---

## 5. 总结

Multi-Agent Flow 将始终坚持 **“0 云端依赖、纯本地单机离线、数据不出本地、配合企业成熟在线看板”** 的核心产品定位。任何演进都必须以提升开发者实际效率与工程可信度为唯一准绳，坚决向过度设计与脱离实际的伪需求说不。
